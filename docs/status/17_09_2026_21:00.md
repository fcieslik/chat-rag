## Aktualna architektura

INTERNET
│
▼
┌─────────────────┐
│ API Gateway │
│ chat-rag-api- │
│ gateway │
│ │
│ /prod │
└────────┬────────┘
│
VPC Link V2
4dok1z
│
▼
┌──────────────────────┐
│ Internal ALB │
│ chat-rag-internal- │
│ alb │
└──────────┬───────────┘
│
▼
┌──────────────────────┐
│ Target Group │
│ chat-rag-internal-tg │
└──────────┬───────────┘
│
▼
┌──────────────────────┐
│ ECS Fargate │
│ chat-rag-cluster │
│ │
│ Chat API │
│ :8000 │
└──────────┬───────────┘
│
▼
OpenAI

A ECS znajduje się w private subnets w:

chat-rag-vpc
10.20.0.0/16

z dwoma AZ:

eu-central-1a
eu-central-1b
Co już jest zrobione?

1. AWS account / podstawy

✅ osobny projekt chat-rag
✅ region eu-central-1
✅ IAM Identity Center
✅ zabezpieczenie root
✅ naming convention
✅ separacja zasobów projektu

2. Networking

✅ VPC

chat-rag-vpc
10.20.0.0/16

✅ 2 public subnets

10.20.0.0/24
10.20.1.0/24

✅ 2 private subnets

10.20.10.0/24
10.20.11.0/24

✅ Internet Gateway
✅ NAT Gateway

ECS działa w private subnetach.

3. Container / compute

✅ ECR:

chat-rag-api

✅ ECS Cluster:

chat-rag-cluster

✅ ECS Service:

chat-rag-api-service

✅ Fargate

0.25 vCPU
0.5 GB RAM

✅ container port 8000

✅ CloudWatch logs:

/ecs/chat-rag-api 4. Secrets

✅ AWS Secrets Manager:

chat-rag/openai

z:

{
"OPENAI_API_KEY": "..."
}

ECS pobiera secret przy uruchamianiu taska.

5. API Gateway

✅ REST API:

chat-rag-api-gateway

Stage:

prod

Publiczny API:

GET /prod/health
POST /prod/chat

Frontend korzysta właśnie z API Gateway, nie z ALB.

6. Private networking backendu

To jest najważniejsza rzecz, którą właśnie skończyliśmy.

VPC Link V2

✅

4dok1z
Internal ALB

✅

chat-rag-internal-alb
Internal Target Group

✅

chat-rag-internal-tg
Security Groups

✅ VPC Link SG
✅ Internal ALB SG
✅ ECS SG

ECS przyjmuje 8000 tylko z Internal ALB SG.

7. Publiczny ALB — usunięty

To jest równie ważne.

Nie mamy już:

Internet
↓
Public ALB
↓
ECS

Usunęliśmy:

chat-rag-alb
chat-rag-api-tg
chat-rag-alb-sg

Czyli backend nie ma bezpośredniego publicznego wejścia przez ALB.

8. Aktualny request flow
   Health
   GET /prod/health
   ↓
   API Gateway
   ↓
   VPC Link
   ↓
   Internal ALB
   ↓
   ECS
   ↓
   GET /health
   ↓
   {"status":"ok"}

Potwierdzone:

/prod/health → 200 OK
Chat

Publicznie:

POST /prod/chat

wewnętrznie:

POST /v1/chat

czyli:

Browser
↓
POST /prod/chat
↓
API Gateway
↓
VPC Link
↓
Internal ALB
↓
ECS
↓
POST /v1/chat
↓
OpenAI

Chat działa przez tę ścieżkę.

Czego jeszcze NIE mamy?

Na ten moment brakuje głównie:

                    INTERNET
                       │
                       ▼
                 ┌──────────┐
                 │   WAF    │   ← NEXT
                 └────┬─────┘
                      ▼
                 API Gateway
                      │
                 VPC Link
                      │
                Internal ALB
                      │
                    ECS

oraz:

Cognito

i:

GitHub
↓
GitHub Actions
↓
ECR
↓
ECS

czyli automatyczny deployment.

Stan projektu w jednym obrazku
INTERNET
│
▼
┌─────────────────┐
│ WAF │
│ 🔜 NEXT │
└────────┬────────┘
│
▼
┌─────────────────┐
│ API Gateway │
│ ✅ │
└────────┬────────┘
│
VPC Link V2
│
▼
┌─────────────────┐
│ Internal ALB │
│ ✅ │
└────────┬────────┘
│
▼
┌─────────────────┐
│ ECS Fargate │
│ ✅ │
└────────┬────────┘
│
▼
OpenAI
│
│
┌──────────────┴──────────────┐
│ │
Cognito 🔜 CI/CD 🔜
Czyli jesteśmy tutaj:
VPC ✅
ECS/Fargate ✅
ECR ✅
Secrets Manager ✅
CloudWatch ✅
API Gateway ✅
VPC Link V2 ✅
Internal ALB ✅
Private ECS ✅
Public ALB 🗑️
WAF 🔜
API improvements 🔜
Cognito 🔜
CI/CD 🔜

## Aktualizacja architektury

Aktualna architektura
┌──────────────────┐
│ Frontend │
│ localhost:5173 │
└────────┬─────────┘
│
▼
┌──────────────────────┐
│ API Gateway │
│ chat-rag-api-gateway │
│ /prod │
└──────────┬───────────┘
│
┌───────┴────────┐
│ │
▼ ▼
┌───────────┐ ┌──────────────┐
│ WAF │ │ API Gateway │
│chat-rag-waf│ │ Throttling │
└───────────┘ └──────────────┘
│
▼
┌──────────────┐
│ VPC Link V2 │
└──────┬───────┘
│
▼
┌──────────────────────┐
│ Internal ALB │
│ chat-rag-internal- │
│ alb │
└──────────┬───────────┘
│
▼
┌──────────────────────┐
│ ECS Fargate │
│ chat-rag-api-service│
│ port 8000 │
└──────────┬───────────┘
│
▼
┌───────┐
│OpenAI │
└───────┘
Co się zmieniło

1. WAF został dodany przed API
   API Gateway
   │
   ▼
   WAF

Web ACL:

chat-rag-waf

Zawiera:

AWS-AWSManagedRulesCommonRuleSet
chat-rag-rate-limit
100 requests / 5 min
per source IP
action: BLOCK

WAF jest przypięty do:

API Gateway → prod 2. API Gateway dostał throttling

Stage prod:

Rate: 10 req/s
Burst: 20

Czyli API Gateway ma teraz własną warstwę kontroli ruchu, niezależną od WAF.

3. Dodaliśmy API Gateway access logging

Powstał osobny CloudWatch Log Group:

/aws/apigateway/chat-rag-api

Logujemy m.in.:

{
"requestId": "...",
"ip": "...",
"method": "GET",
"path": "/health",
"status": "304",
"responseLength": "0",
"waf": "WAF_ALLOW"
}

Dodatkowo skonfigurowana została IAM Role pozwalająca API Gateway wysyłać logi do CloudWatch.

Co pozostało bez zmian

Najważniejsza część architektury nadal jest taka sama:

Browser
↓
API Gateway
↓
VPC Link
↓
Internal ALB
↓
ECS Fargate
↓
OpenAI

Czyli nie przywracamy publicznego ALB i backend ECS nadal nie jest bezpośrednio wystawiony do Internetu.

Aktualny stan infrastruktury
Element Stan
VPC ✅
Public/private subnets + NAT ✅
ECS Fargate ✅
ECR ✅
Internal ALB ✅
VPC Link V2 ✅
API Gateway ✅
WAF ✅
WAF Managed Rules ✅
WAF Rate Limit ✅
API Gateway Throttling ✅
API Gateway Access Logs ✅
CloudWatch Logs ✅
Secrets Manager ✅
Cognito ⏳
Backend CI/CD ⏳

Czyli etap WAF + API Gateway możemy uznać za zamknięty. Następny etap roadmapy to Cognito, a później CI/CD dla backendu.

## Handoff

AWS Chat RAG — HANDOFF DLA KOLEJNEGO LLM

> Data: 17.09.2026
> Region: eu-central-1
> Projekt: `chat-rag`
> Cel: production-like AWS AI Chat + RAG
> Sposób nauki: najpierw AWS Console, później IaC
>
> WAŻNE:
> Użytkownik chce praktycznych instrukcji krok po kroku, ale bez niepotrzebnego rozbijania na dziesiątki mikro-kroków.
> Preferuje logiczne grupowanie czynności.
>
> AKTUALNA STRATEGIA:
> Chcemy teraz domknąć najważniejsze elementy AWS:
>
> 1. WAF + poprawki API Gateway
> 2. Cognito
> 3. CI/CD backendu
>
> Następnie użytkownik chce zamknąć etap infrastruktury AWS i skupić się przede wszystkim na rozwoju samej aplikacji AI/RAG/Agents.

1. CEL PROJEKTU

Budujemy prawdziwie wyglądającą aplikację:

AI Chat + RAG na AWS

Backend:

Express + TypeScript

Frontend:

React / localhost:5173

Backend ma endpointy:

GET /health
POST /v1/chat

Publiczny API contract:

GET /prod/health
POST /prod/chat

Aplikacja komunikuje się z OpenAI.

Docelowo aplikacja ma być bazą do dalszego rozwoju:

Chat
RAG
Agents
Tools
Authentication
Persistence
Observability / LLMOps 2. AKTUALNA ROADMAPA
AWS Infrastructure
Etap Temat Status
0 AWS Account / Workspace Setup ✅ DONE
1 Networking / VPC ✅ DONE
2 Containerized Backend / ECS Fargate ✅ DONE
3 API Gateway ✅ DONE
4 Private ALB / VPC Link / Network Security ✅ DONE
5 WAF + API Gateway Improvements 🔄 NEXT
6 Cognito / Authentication ⏳
7 Backend CI/CD ⏳
8 AWS Cleanup / Final verification ⏳

Po tym użytkownik chce:

zamknąć etap AWS i skupić się na rozwoju aplikacji.

Nie wracamy automatycznie do starego planu z osobnymi fazami:

Frontend hosting
Streaming
Persistence
RAG
Agents
Observability
IaC

Te elementy mogą być później realizowane jako część rozwoju aplikacji, a nie jako obowiązkowe kolejne fazy infrastruktury.

3. PHASE 0 — AWS ACCOUNT / WORKSPACE SETUP
   Status

✅ DONE

Założenia:

osobne zasoby dla projektu
region eu-central-1
naming convention chat-rag-\*
tagowanie projektu
oddzielenie projektu od innych zasobów

Skonfigurowano:

root account
zabezpieczenie root
IAM Identity Center
codzienny dostęp administracyjny przez Identity Center 4. PHASE 1 — NETWORKING / VPC
Status

✅ DONE

VPC:

chat-rag-vpc
CIDR: 10.20.0.0/16

Region:

eu-central-1

AZ:

eu-central-1a
eu-central-1b

Subnets:

Public:
10.20.0.0/24
10.20.1.0/24

Private:
10.20.10.0/24
10.20.11.0/24

Utworzono:

chat-rag-vpc
chat-rag-igw
NAT Gateway

ECS działa w private subnets.

NAT umożliwia ECS wychodzenie do Internetu, np. do OpenAI.

5. PHASE 2 — ECS FARGATE
   Status

✅ DONE

ECR:

chat-rag-api

ECS Cluster:

chat-rag-cluster

ECS Service:

chat-rag-api-service

Task definition:

chat-rag-api

Fargate:

0.25 vCPU
0.5 GB RAM

Container:

8000

Network mode:

awsvpc

Backend:

Express + TypeScript

Endpoint:

GET /health

Response:

{"status":"ok"}

Chat endpoint:

POST /v1/chat 6. ECR / DOCKER

Obecny ręczny deployment backendu:

Docker build
↓
ECR push
↓
new task definition revision
↓
ECS Service update

To jest jeden z powodów, dla których kolejnym dużym etapem po Cognito będzie:

CI/CD backendu.

Użytkownik chce pozbyć się konieczności ręcznego wykonywania wielu czynności po każdym nowym obrazie Docker.

7. CLOUDWATCH

Log group:

/ecs/chat-rag-api

ECS wysyła logi do CloudWatch.

8. SECRETS MANAGER

Secret:

chat-rag/openai

JSON:

{
"OPENAI_API_KEY": "..."
}

ECS pobiera secret przez valueFrom.

WAŻNE:

Nie szukać w ECS osobnej funkcji nazwanej po prostu Secrets.

Secret jest skonfigurowany w Task Definition jako secret environment variable.

9. PHASE 3 — API GATEWAY
   Status

✅ DONE

REST API:

chat-rag-api-gateway

REST API ID:

2780017ujg

Region:

eu-central-1

Stage:

prod

Public invoke URL:

https://2780017ujg.execute-api.eu-central-1.amazonaws.com/prod

Resources:

/
├── OPTIONS
├── /chat
│ ├── OPTIONS
│ └── POST
└── /health
└── GET

Frontend używa:

https://2780017ujg.execute-api.eu-central-1.amazonaws.com/prod/chat

Frontend NIE komunikuje się bezpośrednio z ALB.

10. API GATEWAY — WAŻNA LEKCJA Z PATH

Backend ma:

POST /v1/chat

Public API ma:

POST /prod/chat

Dlatego integration endpoint dla /chat musi wskazywać:

http://internal-chat-rag-internal-alb-1823837357.eu-central-1.elb.amazonaws.com/v1/chat

Analogicznie /health:

http://internal-chat-rag-internal-alb-1823837357.eu-central-1.elb.amazonaws.com/health

To było źródłem problemu podczas migracji na private integration.

Nie próbować ponownie rozwiązywać tego przez ALB URL rewrite.

11. CORS

Dla /chat istnieje:

OPTIONS /chat

jako MOCK integration.

Headers:

Access-Control-Allow-Origin:
http://localhost:5173

Access-Control-Allow-Methods:
POST,OPTIONS

Access-Control-Allow-Headers:
content-type,authorization

Frontend obecnie działa lokalnie.

CORS będzie trzeba później odpowiednio rozszerzyć po dodaniu Cognito / docelowego frontendu.

12. PHASE 4 — PRIVATE ALB / VPC LINK
    Status

✅ DONE

To była ostatnia ukończona faza.

Docelowy flow:

Internet
↓
API Gateway
↓
VPC Link V2
↓
Internal ALB
↓
ECS Fargate 13. VPC LINK

Utworzony:

VPC Link V2

ID:

4dok1z

Używa:

private subnets
chat-rag-vpclink-sg 14. INTERNAL ALB

Nazwa:

chat-rag-internal-alb

Scheme:

Internal

Subnets:

eu-central-1a
eu-central-1b

Listener:

HTTP :80

DNS:

internal-chat-rag-internal-alb-1823837357.eu-central-1.elb.amazonaws.com 15. SECURITY GROUPS
VPC Link SG
chat-rag-vpclink-sg

Inbound:

brak

Outbound:

default
Internal ALB SG
chat-rag-internal-alb-sg

Inbound:

TCP 80
source = chat-rag-vpclink-sg
ECS SG
chat-rag-api-sg

Docelowo:

TCP 8000
source = chat-rag-internal-alb-sg

Stary public ALB SG został usunięty.

16. INTERNAL TARGET GROUP

Nazwa:

chat-rag-internal-tg

Target type:

IP

Protocol:

HTTP

Port:

8000

Health check:

/health

Target:

ECS Fargate task

Status targetu:

Healthy 17. STARY PUBLIC ALB — USUNIĘTY

Stary ALB:

chat-rag-alb

został usunięty.

Stary target group:

chat-rag-api-tg

został usunięty.

Stary security group:

chat-rag-alb-sg

został usunięty.

To oznacza, że backend nie ma już publicznego ALB.

18. POTWIERDZONE TESTY

Po całym cleanupie:

https://2780017ujg.execute-api.eu-central-1.amazonaws.com/prod/health

zwraca:

{"status":"ok"}

Frontend:

http://localhost:5173/

działa.

Chat działa.

Najważniejszy aktualny flow:

Browser
↓
API Gateway
↓
VPC Link V2
↓
Internal ALB
↓
ECS Fargate
↓
OpenAI 19. AKTUALNY CEL — PHASE 5
WAF + API GATEWAY IMPROVEMENTS
Status

🔄 NEXT

Zanim pójdziemy dalej, zabezpieczamy publiczny API Gateway.

Docelowo:

Internet
↓
AWS WAF
↓
API Gateway
↓
VPC Link
↓
Internal ALB
↓
ECS 20. CO CHCEMY ZROBIĆ W PHASE 5
20.1 AWS WAF

Utworzyć:

Web ACL

i przypiąć go do:

API Gateway REST API

Chcemy praktycznie zrozumieć:

Web ACL
rules
AWS Managed Rules
rate-based rule
IP filtering
WAF metrics
WAF logs / monitoring 21. RATE LIMITING

Chcemy mieć dwie warstwy:

API Gateway throttling

Nauczyć się:

rate
burst

oraz throttlingu na:

stage
method

Szczególnie interesuje nas:

POST /chat

ponieważ request może powodować koszt LLM.

Przykładowa konfiguracja edukacyjna może być później:

POST /chat
rate: 2 req/s
burst: 5

Ale nie ustawiaj wartości w ciemno — najpierw pokaż użytkownikowi ekran konfiguracji i wyjaśnij, co dokładnie ograniczamy.

AWS WAF rate-based rule

Dodatkowo:

source IP
↓
too many requests
↓
BLOCK

To ma być osobna warstwa od API Gateway throttling.

22. API GATEWAY IMPROVEMENTS

W tej fazie warto również uporządkować:

throttling
access logging
stage settings
CORS
request validation, jeśli będzie potrzebna
timeout / streaming configuration
poprawne response handling
monitoring API Gateway

Nie należy robić zmian tylko dla samego "odhaczenia" funkcji.

Każda zmiana powinna mieć praktyczne uzasadnienie.

23. STREAMING

Backend /v1/chat obsługuje streaming.

API Gateway /chat ma:

Response transfer mode:
Stream

Streaming działa w aplikacji.

Nie trzeba teraz robić osobnej fazy infrastrukturalnej tylko dla streamingu.

Jeżeli podczas WAF/API Gateway improvements pojawi się problem ze streamingiem, wtedy go rozwiązujemy.

24. PHASE 6 — COGNITO
    Status

⏳

Po zakończeniu WAF/API Gateway przechodzimy do Cognito.

Docelowo:

Browser
↓
Cognito
↓
JWT
↓
API Gateway
↓
VPC Link
↓
Internal ALB
↓
ECS

Cognito ma odpowiadać za:

user registration
login
JWT
authentication

API Gateway ma weryfikować JWT.

Backend nie powinien implementować całego login/password flow samodzielnie.

Po Cognito trzeba będzie dostosować CORS:

Authorization

oraz frontend.

25. PHASE 7 — BACKEND CI/CD
    Status

⏳

To jest bardzo ważny etap dla użytkownika.

Obecnie deployment:

Docker build
↓
ECR push
↓
new task definition revision
↓
ECS service update

jest zbyt ręczny.

Chcemy:

git push
↓
GitHub Actions
↓
Docker build
↓
ECR push
↓
new ECS task definition
↓
ECS deployment
↓
rolling update 26. CI/CD — WAŻNE CELE EDUKACYJNE

Użytkownik chce zrozumieć praktycznie:

GitHub Actions
OIDC między GitHub i AWS
IAM role dla GitHub Actions
ECR authentication
Docker build/push
ECS deployment
task definition revision
rolling deployment
deployment verification
rollback

Preferowane rozwiązanie: GitHub Actions + AWS OIDC.

Nie używać długoterminowych AWS access keys w GitHub Secrets, jeśli nie ma ku temu konkretnego powodu.

27. PO CI/CD — ZAMYKAMY ETAP AWS

Po:

WAF + API Gateway
↓
Cognito
↓
Backend CI/CD

użytkownik chce zamknąć główny etap infrastruktury AWS.

Nie należy wtedy automatycznie rozpoczynać:

S3
CloudFront
IaC

tylko dlatego, że były w pierwotnej roadmapie.

Priorytet zmienia się na:

DEVELOPMENT OF THE APPLICATION 28. DALSZY ROZWÓJ APLIKACJI

Po zamknięciu AWS skupiamy się na funkcjonalności.

Kolejne tematy mogą obejmować:

Chat
↓
Conversation history
↓
Persistence
↓
RAG
↓
Bedrock Knowledge Bases
↓
Agents
↓
Tools
↓
Guardrails
↓
Evaluation
↓
Observability / LLMOps

Kolejność może być zmieniana zależnie od rozwoju aplikacji.

Najważniejsze jest teraz:

AWS ma być platformą, na której aplikacja działa — nie celem samym w sobie.

29. RAG — PRZYSZŁY KIERUNEK

Docelowo:

Documents
↓
S3
↓
Bedrock Knowledge Base
↓
Embeddings
↓
Vector search
↓
Retrieval
↓
Grounded response

Użytkownik chce praktycznie zrozumieć:

ingestion
chunking
embeddings
retrieval
vector search
grounding
citations
RAG evaluation 30. AGENTS — PRZYSZŁY KIERUNEK

Po podstawowym RAG:

Chat
↓
Agent
↓
Tools
↓
RAG
↓
AWS services

Tematy:

agent architecture
tool calling
planning
agent state
guardrails
human-in-the-loop
evaluation 31. OBSERVABILITY / LLMOPS — PRZYSZŁY KIERUNEK

Docelowo:

Request
↓
API
↓
LLM
↓
Telemetry

Monitorowane:

latency
token usage
cost
errors
model calls
retrieval
agent execution
traces

MLflow może być później osobnym tooling/platform layer.

Nie jest wymagane jako element AWS.

32. IAC

IaC było pierwotnie planowane jako końcowa faza.

Obecnie:

NOT NOW

Użytkownik chciał najpierw nauczyć się AWS przez Console.

Jeśli IaC wróci później, możliwe opcje:

Terraform
AWS CDK
CloudFormation

Ale dopiero po zakończeniu praktycznej nauki i developmentu.

33. NAJWAŻNIEJSZE ZASADY DLA KOLEJNEGO LLM
    Nie zaczynaj projektu od początku.

Istnieją już:

VPC
ECS
ECR
Internal ALB
Internal Target Group
VPC Link V2
API Gateway
Secrets Manager
CloudWatch

Nie twórz ich ponownie.

Nie przywracaj publicznego ALB.

Publiczny backend ALB został celowo usunięty.

Aktualny backend flow
API Gateway
→ VPC Link V2
→ Internal ALB
→ ECS
Aktualny publiczny API
GET /prod/health
POST /prod/chat
Aktualne wewnętrzne endpointy
GET /health
POST /v1/chat
Aktualny frontend
http://localhost:5173/

i komunikuje się z:

https://2780017ujg.execute-api.eu-central-1.amazonaws.com/prod/chat 34. AKTUALNY STATUS
PHASE 0 AWS Account / Workspace ✅
PHASE 1 VPC / Networking ✅
PHASE 2 ECS / Fargate ✅
PHASE 3 API Gateway ✅
PHASE 4 Private ALB / VPC Link ✅

PHASE 5 WAF + API Gateway 🔄 NEXT
PHASE 6 Cognito ⏳
PHASE 7 Backend CI/CD ⏳

---

AWS INFRASTRUCTURE → prawie zamknięte

Następnie:
APPLICATION DEVELOPMENT 35. NAJBLIŻSZY KROK

Nie przechodź jeszcze do Cognito.

Najpierw:

PHASE 5
WAF + API Gateway Improvements

Kolejność:

1. AWS WAF Web ACL
2. AWS Managed Rules
3. WAF rate-based rule
4. API Gateway throttling
5. API Gateway access logging / monitoring
6. sprawdzenie CORS
7. sprawdzenie streaming configuration
8. testy

Po zakończeniu:

WAF + API Gateway
↓
Cognito
↓
GitHub Actions / ECR / ECS CI/CD
↓
AWS stage CLOSED
↓
Application development 36. OSTATNI POTWIERDZONY STAN

Ostatni test użytkownika:

https://2780017ujg.execute-api.eu-central-1.amazonaws.com/prod/health
→ {"status":"ok"}

http://localhost:5173/
→ działa

Chat również działa.

Następna czynność: rozpocząć konfigurację AWS WAF dla chat-rag-api-gateway.

To jest wersja, którą dałbym kolejnemu LLM jako **source of truth**. Najważniejsza zmiana względem starej roadmapy to świadome przejście z „budujemy każdy możliwy element AWS” na **„zabezpieczamy + automatyzujemy AWS, a potem rozwijamy produkt”**.
